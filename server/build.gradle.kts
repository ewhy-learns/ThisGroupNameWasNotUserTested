// Server build removed for client-only prototype. If you want to re-enable the server,
// restore the original build.gradle.kts from version control or re-add the Ktor configuration.

// Intentionally left blank.

/*
 * Server module removed for client-only prototype.
 * The original Ktor server source was intentionally removed from the prototype to keep the
 * repository focused on the client-only web app. If you need to restore the server later,
 * you can recover it from version control history or re-enable the server project in
 * settings.gradle.kts and re-add the original sources.
 */

package com.example.demo1

// Placeholder so the file exists but does nothing. Server has been removed from the prototype.
fun main() {
    println("Server module removed in client-only prototype. No server will be started.")
}

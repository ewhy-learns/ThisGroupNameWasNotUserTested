/* User data removed for client-only prototype. */
package com.example.demo1.users

// Placeholder type kept to avoid accidental references; server is decommissioned in this prototype.
data class UserPlaceholder(val id: String)
